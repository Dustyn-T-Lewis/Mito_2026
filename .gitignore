# macOS
.DS_Store

# R session state / IDE
.Rhistory
.RData
.Rdata
.Rproj.user/
.Renviron
.httr-oauth
Rplots.pdf

# R package build artifacts
*.tar.gz
*.Rcheck/

# renv (if added later)
renv/library/
renv/local/
renv/staging/

# Rendered pipeline outputs — regenerate by running the R scripts
# (keep the directory placeholders so the layout is visible on clone)
**/b_reports/*
!**/b_reports/.gitkeep

# Frozen pipeline data artifacts — regenerate by running stages 01-05
**/c_data/*
!**/c_data/.gitkeep

# Root-level duplicates of 00_input/ files (canonical copies live in 00_input/)
/DAP_lists.xlsx
/Proteomics of PE with mito transfer*.xlsx

# Operational working notes (not project documentation)
/*_PROMPT.md

# Writeup / documentation deliverables (live locally; README.md is the only
# tracked doc for repo orientation)
**/*.docx
**/*.pdf
**/RESULTS_INTERPRETATION.*
**/METHODS*.md
**/MANUSCRIPT*.md
**/AUDIT*.md
**/NOTES*.md
